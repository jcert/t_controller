import RPIO


class encoder(pinA, pinB, pinC):
	ENCODER_TICKS_TURN = 20
	#pins of the encoder
	A = pinA 
	B = pinB 
	C = pinC
	def update()
		read A
		read b
		is different state from before?
		FSM magic -> which direction it moved to
		add that to total distance
		update estimated velocity


	def get_speed()
	def get_dist()



